# Tasks

- [ ] Frontend: Create `StoreContextBanner` component (optional, or inline) <!-- id: 1 -->
- [ ] Frontend: Update `PromotionsView.vue` to display Store Info <!-- id: 2 -->
  - Fetch full store details (Name, Image/Logo) if not already available in `stores` list.
  - Display Banner above Filter Bar.
