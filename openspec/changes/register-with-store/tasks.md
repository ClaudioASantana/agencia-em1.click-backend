# Tasks

- [ ] Backend: Update `UsersService.create` to handle `storeName` <!-- id: 1 -->
  - If `storeName` is present, create Establishment.
  - Link new User to new Establishment.
- [ ] Frontend: Update `RegisterModal.vue` <!-- id: 2 -->
  - Add `storeName` input.
  - Rename `name` label to "Seu Nome".
  - Send `storeName` in API payload.
